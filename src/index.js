// .forEach()

// const circles = document.querySelectorAll('.circle');

// console.log(circles)

// array.forEach(pickanyname => butusesamenamehere)

// circles.forEach(circle => circle.style.backgroundColor = 'red')

// const names = ['Chris', 'Jesse', 'Mark', 'Pete', 'Steve']

// names.forEach(name => console.log(name))

const scores = [32, 543, 43, 22, 111];

scores.forEach((score) => {
  console.log(score + 2);
});
